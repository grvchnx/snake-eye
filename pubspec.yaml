# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

name: ultralytics_yolo
description: SnakeEye — 🐍👀
version: 0.0.1
homepage: https://github.com/grvchnx/snake-eye
repository: https://github.com/grvchnx/snake-eye
issue_tracker: https://github.com/grvchnx/snake-eye/issues

topics:
  - computer-vision
  - machine-learning

environment:
  sdk: ^3.8.1
  flutter: ">=3.32.1"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.5
  plugin_platform_interface: ^2.1.8
  shared_preferences: ^2.2.0
  url_launcher: ^6.3.2
  wakelock_plus: ^1.2.0

dev_dependencies:
  coverage: ^1.15.0
  flutter_lints: ">=5.0.0 <7.0.0"
  flutter_test:
    sdk: flutter
  mockito: ^5.6.4
  very_good_analysis: ">=6.0.0 <11.0.0"

flutter:
  plugin:
    platforms:
      android:
        package: com.ultralytics.yolo
        pluginClass: YOLOPlugin
      ios:
        pluginClass: YOLOPlugin
  assets:
    - assets/focus_reticle.png
